// creating this as we donot require a page.js for this 
// we just created this slot so that we can show the selected image in  pagefeed main page instead of a 
// new page that is why we created intercepted route for /page-feed/[photoid] instead of just [photoid]  

export default function modalDefault(){
    return null
}